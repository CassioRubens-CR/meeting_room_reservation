import type { RouteDef } from '..'

const path = '/admin/reservations'
const componentName = 'AdminReservationsPage'
const isProtected = true

export default { path, componentName, isProtected } satisfies RouteDef
